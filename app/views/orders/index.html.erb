<a href="/">Back to Main Page</a><br>
<a href="/customers">Customers</a><br>
<a href="/orders">Back to Orders</a><hr>


<h1>Order Up!</h1>

<% @orders.where(paid: true).each do |order| %>
<h3>Order #:<%= link_to "#{order.id}", "/orders/#{order.id}" %>
  <br>Special Instructions: <%= order.special_instructions %></h3>
<%= link_to "Update Order", "/orders/#{order.id}/edit" %> | <%= link_to "Delete Order", "/orders/#{order.id}", method: :delete %><hr>

<% end %>
